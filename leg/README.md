# leg (legacy LPG)

Sample LEG language grammar formerly used with Eclipse IMP.

Harnessed for parse-level CI: Eclipse `ILexer`/`IParser`/`SymbolTable` deps removed;
package is `lpg.grammars.leg`.

```bash
LPG_BIN=../lpg2/build/lpg-v2.2.03 bash harness/run-one.sh leg
```
