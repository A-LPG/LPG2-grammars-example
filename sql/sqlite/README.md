# sql/sqlite

Parse-level LPG port of antlr/grammars-v4 `sql/sqlite` (Wave D curated subset).

Curated examples under `examples-curated/` cover SELECT/INSERT/CREATE/DROP/UPDATE/DELETE and JOIN.

```bash
bash harness/run-one.sh sql/sqlite
```
