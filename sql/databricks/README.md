# sql/databricks

Auto-ported from antlr/grammars-v4 `sql/databricks` via `tools/antlr2lpg.py`.

Structural rule transfer (EBNF expanded). May need LALR conflict fixes.
Start symbol: `databricks_file`.
