/*
 * CTO model — transaction / event
 */
namespace org.sample.transaction

import org.sample.car.Car

transaction Buy {
  --> Car asset
  o String newValue
  o String foo
  o Integer bar
}

event SampleEvent {
  --> Car asset
  o String oldValue
  o String newValue
}
