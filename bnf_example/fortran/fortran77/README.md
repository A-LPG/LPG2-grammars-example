# fortran/fortran77

Auto-ported from antlr/grammars-v4 `fortran/fortran77` via `tools/antlr2lpg.py`.

Structural rule transfer (EBNF expanded). May need LALR conflict fixes.
Start symbol: `program`.
