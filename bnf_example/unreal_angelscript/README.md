# unreal_angelscript

Auto-ported from antlr/grammars-v4 `unreal_angelscript` via `tools/antlr2lpg.py`.

Structural rule transfer (EBNF expanded). May need LALR conflict fixes.
Start symbol: `script`.
