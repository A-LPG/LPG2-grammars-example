# jdt.core (legacy)

Eclipse JDT-era Java grammar and generated helpers under `.lpg/`. **Reference only** — not wired to the Java parse harness (`harness_skip`).

For a harnessed Java subset port, see [`java/java/`](../java/java/). Legacy GJava sources live in [`../java/`](../java/).
