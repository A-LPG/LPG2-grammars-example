# sql/cockroachdb

Auto-ported from antlr/grammars-v4 `sql/cockroachdb` via `tools/antlr2lpg.py`.

Structural rule transfer (EBNF expanded). May need LALR conflict fixes.
Start symbol: `cockroachdb_file`.
