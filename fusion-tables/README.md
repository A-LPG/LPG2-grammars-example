# fusion-tables

Auto-ported from antlr/grammars-v4 `fusion-tables` via `tools/antlr2lpg.py`.

Structural rule transfer (EBNF expanded). May need LALR conflict fixes.
Start symbol: `fusionTablesSql`.
