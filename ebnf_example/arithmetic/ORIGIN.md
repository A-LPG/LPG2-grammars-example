# Origin

- Upstream: [antlr/grammars-v4](https://github.com/antlr/grammars-v4) `arithmetic`
- Source ref: `grammars-v4/arithmetic`
- License: see upstream `.g4` / directory headers (often BSD); this LPG port retains attribution.
- Quality: `language_port` (see `status.json` / `catalog.json` quality schema)

This directory is an LPG2 parse-level adaptation (`%Options ebnf`), not a byte-for-byte Antlr grammar.
Classic-BNF twin: `grammars-example/bnf_example/arithmetic/`.
