# Origin

- Upstream: [antlr/grammars-v4](https://github.com/antlr/grammars-v4) `datalog`
- Source ref: `grammars-v4/datalog`
- License: see upstream `.g4` / directory headers (often BSD); this LPG port retains attribution.
- Quality: `language_port` (see `status.json` / `catalog.json` quality schema)

This directory is an LPG2 parse-level adaptation, not a byte-for-byte Antlr grammar.

This directory is an LPG2 parse-level adaptation (`%Options ebnf`).
Classic-BNF twin: `grammars-example/bnf_example/datalog/`.
