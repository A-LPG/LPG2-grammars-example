# tsv (EBNF)

Structural LPG port of grammars-v4 `tsv/tsv.g4` (`tsvFile` / `hdr` / `row` / `field`).

Lexer: `TEXT`, `STRING`, `TAB`, `EOL`.

```bash
bash ../harness/run-one.sh tsv
```


Classic-BNF twin: [`../../bnf_example/tsv/`](../../bnf_example/tsv/).
