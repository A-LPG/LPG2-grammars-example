begin_problem(contra).

list_of_descriptions.
name().
author().
status(unsatisfiable).
description().
end_of_list.

list_of_symbols.
functions[plus,s,zero].
sorts[nat].
end_of_list.

list_of_declarations.
even(zero).
end_of_list.

list_of_formulae(axioms).
formula(forall([nat(y)],equal(plus(y,zero),y))).
end_of_list.

end_problem.
