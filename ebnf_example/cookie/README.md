# Cookie (EBNF pilot)

Parse-level port of [antlr/grammars-v4 `cookie/`](https://github.com/antlr/grammars-v4/tree/master/cookie) using `%Options ebnf`.

Classic-BNF twin: [`../../bnf_example/cookie/`](../../bnf_example/cookie/).

## License

BSD (see original `cookie.g4` header).

## Verify

```bash
bash harness/run-one.sh cookie
```
