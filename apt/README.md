# APT (LPG)

Parse-level port of [antlr/grammars-v4 `apt/`](https://github.com/antlr/grammars-v4/tree/master/apt).

Upstream has no examples; `examples/` are synthesized.

## Verify

```bash
../harness/run-one.sh apt
```
